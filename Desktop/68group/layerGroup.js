

const cities = L.layerGroup([test]);

const parks = L.layerGroup([crownHill, rubyHill]);

const shopsContainer = L.layerGroup([shops, shops2]);

const MagazinContainer = L.layerGroup([magazin, magazin2])

const carsContainer = L.layerGroup([cars, cars2])

const makatbContainer= L.layerGroup([maktab, maktab2])